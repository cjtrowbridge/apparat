## TODO

This file is an asynchronous inbox for user-captured tasks and questions. It is not execution authority, a roadmap, or an approved plan. Agents should read it for context, but only act on items here when the user explicitly asks to pull one into the active plan.

- [ ] the selected tab on android always jumps to the left edge of the screen when you let go of it, which means its never possible to click on anything to the left of that. the selected tab should not cause the tab buttons to jump. the user needs to be able to scroll back and forth and let go and then click on any tab they want, without it just automatically jumping around when they let go.
- [ ] is it possible to take the same hash of the binary that's shown in git for the file, so we can compare our binary to the current latest version on github? This could let us move immediately to a model of notifying/updating when a new version is available rather than waiting for the beta to start doing that.
- [ ] let's add a view fieldset in settings with a checkbox for show dev overlay. this should be unchecked by default but show a new debugui floating/draggable box with statistics like window size, frame rate, ups, resource usage, system resource usage, binary path, runtime path, etc. This new view fieldset should also include a theme dropdown but the only option for now should be dark mode. this dropdown doesn't need to be wired into anything at this point.
- [ ] we need a screen width threshhold where the right columns disappear from the view. in this setuation, the left columns are there by default, and then the user clicks a thread which hudes the fullscreen left-column with the full-screen right column; this also needs an extra "<- Back" button at the top when the left-column is in full-screen mode. Let's make this threshold be set by the configuration manager and add new ui to a later user config phase so that the user will be able to set that to whatever they want.
- [ ] can we already make this new/temporary update-in-place feature apply to all the os/architecture build types, so they can all start updating themselves immediately? it seems like linux and windows binaries should deploy a simple (os-specfici) update script that downloads the new binary to a temporary filename next to the main binary, tells the main binary to exit, and then moves the temporary to replace the main binary and launches the new binary.  If anything fails, just re-launch the old binary with some kind of flag to let it know the update failed. then show a toast about the update failing when that flag is set at launch.
