# Journal: 2026-07-09

## Repo Work Log (Required)

- 2026-07-09 Continued Phase 5 Android GUI validation: Pixel ADB showed the direct `GoNativeActivity` APK still reached only the splash path and failed with `internal/ui: Run is not implemented for GOOS=android`; promoted the wrapper/AAR-style fallback, added tracked `android/apparat` wrapper sources and `cmd/apparatmobile`, added `scripts/android_wrapper.py`, rebuilt the canonical `releases/android/arm64/apparat/latest.apk`, and verified package metadata, v1/v2/v3 signing, `arm64-v8a`, portrait metadata, and 16 KB native page alignment. Final on-device wrapper visual validation is still blocked because ADB escalation is unavailable in the current environment.
- 2026-07-09 Diagnosed and fixed the black screen rendering issue on Android: tracked the crash in `EbitenView` to a JNI startup lag where `deviceScale()` returned `0.0` before initialization completed, causing `pxToDp` to return `Double.POSITIVE_INFINITY`. This translated to `math.MaxInt64` in the Go layout handler, causing Ebitengine's atlas allocator to panic. Patched `scripts/android_wrapper.py` to intercept and replace `EbitenView.java`'s `pxToDp` scale division to default to `1.0` if `scale <= 0.0`. Added defensive checks in Go `Layout` inside `internal/adapters/gui/ebiten_shell.go` to ignore layout bounds larger than 100,000. Rebuilt the APK, installed it successfully on the Pixel Tablet, verified the Go runtime completes initialization successfully via `last_run.log`, and confirmed Ebitengine rendering loops continuously without crashing.
- 2026-07-09 Solved the underlying black screen layout calculation by introducing explicit Java-to-Go scale propagation. Discovered that Ebitengine's internal `deviceScaleFactor` remained `0.0` because gomobile's native context query returned false under our custom `MainActivity` bootstrap, leading to a `0x0` canvas allocation. Added `SetDeviceScale` on the Go side (`ui_mobile.go`, `mobile.go`) and configured `EbitenView.onLayout` in `scripts/android_wrapper.py` to push the real Android screen density to Go. Also added a `replace` directive for `github.com/hajimehoshi/ebiten/v2` in the root `go.mod` to guarantee the build compiles the local modified Ebitengine copy. Verified via screenshot that the Apparat console GUI renders perfectly and sharp on the target device.
- 2026-07-09 Executed plan `2026-07-09-14-25-00_refactor-android-rendering-and-fit-screen`: replaced all custom scale-propagation hacks with `go.Seq.setContext(getApplicationContext())` in `MainActivity.java`, which gives Ebitengine's native JNI display query the Android context it needs. Reverted `SetDeviceScale` from `ui_mobile.go` and `ebitenmobileview/mobile.go`, reverted the `Begin()` framebuffer query hack in `graphics.go`, stripped all debug logging and `pxToDp` overrides from `android_wrapper.py`, removed the transparent-background hack and verbose lifecycle logging from `MainActivity.java`, and cleaned debug instrumentation from `ebiten_shell.go`. Removed `android:screenOrientation="portrait"` and added `RECORD_AUDIO` permission. Added touch input support via `inpututil.AppendJustPressedTouchIDs` feeding into `tabIndexAt`. Added gamepad controller parity via `updateGamepad()` mapping L1/R1 to tab switching and R2 to push-to-talk. Verified on-device: full-screen rendering and touch tab switching confirmed via screenshot.

