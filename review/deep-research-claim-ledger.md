# Deep-Research Claim Ledger

Snapshot: `8095ee4d2845b31ee2272ac5849a03bf0612f773`. “Verified” means verified against that project-owned snapshot, not that a recommendation has been approved.

| ID | Report claim | Evidence class and source locator | Snapshot status | Outcome and confidence | Proposed disposition |
| --- | --- | --- | --- | --- | --- |
| DR-01 | The current ID generator is not a canonical ULID. | Direct observation: `internal/database/database.go:98-105` formats UTC milliseconds and ten random bytes as hexadecimal; `ROADMAP.md` Phase 7 §6.4 requires injectable clock and ID ports but does not select an ID encoding. | Unchanged | Verified, high confidence. The format is not canonical ULID; whether ULID is required is a product decision. | Needs decision: ID contract before new durable/external ID consumers. |
| DR-02 | SQLite ownership and schema creation are transitional rather than centralized. | Direct observation: `internal/app/runtime.go:136-150` opens the database and passes `db.SQL`; `internal/cluster/cluster.go:38-46` and `internal/messaging/messaging.go:29-35` create tables directly; `internal/database/database.go:46-82` exposes `DB.SQL`. | Unchanged | Verified, high confidence. | Accept: clarify and enforce existing Phase 7 §4.1, §4.5, and §4.8. |
| DR-03 | Replay detection treats every insert error as a duplicate. | Direct observation: `internal/messaging/messaging.go:50-56` returns `(true, nil)` for every non-nil error from the replay insert. | Unchanged | Verified, high confidence. | Accept: correct or retire this transitional behavior before it backs network/retry semantics. |
| DR-04 | Startup/shutdown lack the intended transactional lifecycle. | Direct observation: `internal/app/runtime.go:95-157` returns from failed initialization stages without reverse cleanup; `internal/app/runtime.go:159-169` primarily closes the database. `ROADMAP.md` Phase 7 §5.2-§5.7 already specifies lifecycle states, unwind, ordering, and repeatable close. | Unchanged | Verified, high confidence. | Accept as confirmation of existing Phase 7 scope; no separate new architecture workstream. |
| DR-05 | The application runtime owns concrete GUI/HUD state. | Direct observation: `internal/app/runtime.go:12-20` imports `internal/adapters/gui` and `internal/hud`; `internal/app/runtime.go:43` stores `hud.Shell`; `internal/app/runtime.go:200-201` exposes HUD snapshot. `ROADMAP.md` Phase 7 §1.2 and §13.1 require the inverse boundary. | Unchanged | Verified, high confidence. | Accept: make the explicit removal of GUI/HUD imports a Phase 7 acceptance condition. |
| DR-06 | `identity.Classify` is filesystem presence, not trust validation. | Direct observation: `internal/identity/identity.go:158-168` checks only existence of two files. | Unchanged | Verified, high confidence. | Accept: label it local bootstrap state and require strict validation/migration before it can influence trust. |
| DR-07 | Android readiness can report success after failed initialization. | Direct observation: `cmd/apparatmobile/mobile.go:55-57` always returns `true`; the same file's initialization path can return after an error. | Unchanged | Verified, high confidence. | Accept: require truthful Android lifecycle/readiness behavior in Phase 7's existing platform hardening scope. |
| DR-08 | Function-comment compliance is nearly absent. | Deterministic source-level measurement documented in `review/README.md`: 35 non-test files, 243 function declarations, 2 immediately preceded by `//`. | Unchanged | Verified as a narrow presence measurement, high confidence. Comment usefulness is not mechanically established. | Needs user decision: prospective targeted requirement versus project-wide backfill. |
| DR-09 | Historical completion claims exceeded semantic implementation evidence. | The current `ROADMAP.md` is already a reconciled forward plan; establishing historical semantic mismatch would require a broader audit of past plans, tests, and commits than this conversion review. | Not fully audited | Plausible but unverified for governance-change authority. | Defer: retain as a review heuristic, not a mandatory Phase 7 gate. |
| DR-10 | SQLite foreign keys need per-connection treatment. | Material upstream assertion in `deep-research.md` Part X cites SQLite documentation; the existing Phase 7 plan already requires per-connection foreign keys in §4.2. No new pinned dependency behavior is required to preserve that requirement. | Unchanged | Supported for planning, medium confidence at this review's evidence level. | Accept existing Phase 7 requirement; no additional upstream research now. |

## Interpretation rule

The evidence column records what is observable. The disposition column is not evidence and must be read with `deep-research-decisions.md`. In particular, DR-01 and DR-08 require a user policy choice before a Phase 7 amendment can make their proposed remedy mandatory.
